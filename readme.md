# Web Development with PHP and MySQL (Advanced - OOP) 

A Simple PHP Application Structure Using OOP Approach

## Installation

Open up terminal and run following command:

```
cd openxox-todo/app && composer install
```
